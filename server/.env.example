# exist server 환경변수
PORT=4000

# AI agent — 설정하면 Claude API로 nowbar 브리핑 생성, 없으면 규칙 기반 폴백
# https://platform.claude.com 에서 발급
ANTHROPIC_API_KEY=

# mediasoup — 배포 시 서버 공인 IP (개발은 비워두면 127.0.0.1)
ANNOUNCED_IP=
RTC_MIN_PORT=40000
RTC_MAX_PORT=40100
